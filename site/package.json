{
  "name": "@asymmetric-effort/jsonlint-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "test": "npx playwright test",
    "test:install": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "@asymmetric-effort/specifyjs": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "typescript": "^5.5.0",
    "vite": "^6.4.0"
  }
}
